# Trustnode Edge Frontend

Modern React dashboard for gateway controls and live readings.

## Start

```powershell
npm install
npm run dev
```

The Vite proxy points `/api` and `/ws` to backend `127.0.0.1:8000`.
