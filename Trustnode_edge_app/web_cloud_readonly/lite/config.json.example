{
  "_comment": "Copy this file to config.json and fill in real values. Both supabase_url and supabase_anon_key are PUBLIC by design — security comes from RLS, not from hiding these. customer_label is shown in the header. tenant_id is informational only (RLS still enforces it from the user's lite_profiles row).",
  "supabase_url": "https://tsfreqjcrgbxdwvmxeuk.supabase.co",
  "supabase_anon_key": "eyJhbGciOiJIUzI1NiIs...REPLACE_WITH_YOUR_ANON_KEY",
  "customer_label": "TrustNode",
  "tenant_id": "default",
  "show_dashboard_tab": true,
  "show_alarms_tab": true,
  "show_power_tab": true,
  "live_poll_ms": 2000,
  "history_default_limit": 300,
  "default_chart_range_minutes": 60
}
